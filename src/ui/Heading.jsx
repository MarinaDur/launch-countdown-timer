import { styled, css } from "styled-components";

const Heading = styled.h1`
  font-size: 1.8rem;
  letter-spacing: 6px;
  text-align: center;
  font-weight: 700;
  line-height: 1.5;
`;

export default Heading;
